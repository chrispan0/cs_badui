services:
  app:
    image: cpan0/cs_badui:latest
    pull_policy: always
    env_file:
      - .env
    environment:
      - MONGODB_URI=${MONGODB_URI}
    ports:
      - "3008:3000"
    restart: unless-stopped
